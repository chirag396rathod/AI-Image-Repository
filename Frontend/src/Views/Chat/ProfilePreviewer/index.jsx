import React from "react";
import { ProfilePreviewerContainer } from "../styled";

const ProfilePreviewer = () => {
  return (
    <ProfilePreviewerContainer>ProfilePreviewer</ProfilePreviewerContainer>
  );
};

export default ProfilePreviewer;
